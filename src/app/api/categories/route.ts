import { NextResponse } from "next/server";
import {}
import { prisma } from "@/src/lib/db";

export async function GET(){
    try {
        const  {userID} = auth()
    } catch (error) {
        
    }
}